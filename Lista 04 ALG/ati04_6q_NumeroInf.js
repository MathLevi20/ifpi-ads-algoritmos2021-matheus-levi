const prompt = require('prompt-sync')()

function main() {
    //entrada
    num_a = Number (prompt('Escolha um numero:'))
    //processamento
    
    cont = 0
    while(num_a >= i ){
    num_a = num_a / 10  
        cont ++
        // saida
        console.log(`O numero Informado tem ${cont} Digitos`)  
    }
   
}

    
main()
